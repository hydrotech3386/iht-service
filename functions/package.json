{
  "name": "service-schedule-functions",
  "version": "1.0.0",
  "description": "Scheduled jobs for Service Schedule: nightly RTDB backup + daily maintenance-contract reminders",
  "main": "index.js",
  "engines": { "node": "22" },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  }
}
