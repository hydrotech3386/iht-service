{
  "name": "service-schedule-functions",
  "version": "1.0.0",
  "description": "Scheduled nightly backup of Service Schedule Firebase RTDB to Cloud Storage",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^6.0.0"
  }
}
